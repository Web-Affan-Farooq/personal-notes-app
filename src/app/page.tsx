import { Sidebar } from "@/components/layout";

const Home = () => {
  return (
    <main>
      <article className="w-full h-full">
        <Sidebar />
      </article>
    </main>
  );
};

export default Home;
